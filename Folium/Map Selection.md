Folium allows for retrieval of maps using regular coordinates
In python, the syntax is `m = folium.map(location=(N/S, E/W)`

### Appearance of Map:
By default, Folium shows the `openStreetMap` [[Tiles]] of the parsed location
